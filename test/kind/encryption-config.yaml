apiVersion: apiserver.config.k8s.io/v1
kind: EncryptionConfiguration
resources:
  - resources:
      - secrets
      - configmaps
    providers:
      - kms:
          apiVersion: v2
          name: kagimori
          endpoint: unix:///tmp/kagimori.sock
          timeout: 3s
      - identity: { }
